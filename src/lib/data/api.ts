// Data-access facade used by every page/component. Picks the Supabase-backed
// implementation (supabaseApi.ts) when VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY
// are configured, otherwise the local dev-mode implementation (localApi.ts,
// localStorage-backed, seeded from src/data/seed) so the app is fully
// clickable with zero backend setup. Both implementations share the same
// function signatures and types (see types.ts) — pages only ever import from
// this file and never need to know which backend is active.
// See docs/ARCHITECTURE.md.

import { isSupabaseConfigured } from "./supabaseClient";
import * as local from "./localApi";
import * as remote from "./supabaseApi";

const impl = isSupabaseConfigured ? remote : local;

export const {
  ApiError,
  signUp,
  signIn,
  signOut,
  getCurrentUser,
  getTenantSummary,
  getTenantVerificationDetails,
  updateTenantProfile,
  updateTenantPreferences,
  addTenantArea,
  removeTenantArea,
  getLandlordProfile,
  getLandlordPublicProfile,
  updateLandlordCompanyName,
  listProperties,
  listPropertiesForLandlord,
  getProperty,
  createProperty,
  updateProperty,
  deleteProperty,
  addPropertyPhoto,
  getMatchesForTenant,
  listMarketplaceTenants,
  listApplicationsForTenant,
  listApplicationsForLandlord,
  createApplication,
  updateApplicationStatus,
  listConversationsForUser,
  getOrCreateConversation,
  listMessages,
  sendMessage,
  toggleSavedProperty,
  listSavedProperties,
  toggleSavedTenant,
  listSavedTenants,
  createInvitation,
  listInvitationsForTenant,
  listInvitationsForLandlord,
  respondToInvitation,
  toggleTenantInterest,
  listInterestsForLandlord,
  createPassportShare,
  listPassportShares,
  revokePassportShare,
  recordPassportView,
  listPassportViews,
  listPassportViewsWithViewers,
  createLandlordReview,
  listLandlordReviews,
  listSubscriptionPlans,
  updateSubscriptionPlan,
  listNotifications,
  markNotificationRead,
  notifyOnce,
  getSubscription,
  setSubscriptionTier,
  listApplicantsForProperty,
  getAdminMetrics,
  getUserEmail,
  getOwnAutoPaymentEnrollment,
  getOwnPaymentSetup,
  startCheckout,
  listRentIncentives,
  upsertRentIncentive,
  listJurisdictionRules,
  updateJurisdictionRule,
  setAutoPaymentEnrollment,
  recordPayment,
  listPaymentVerificationsForTenant,
  listPaymentVerificationsForLandlord,
  listPerfectPayMilestones,
  updatePerfectPayMilestone,
  listRewardEvents,
  getAdFrequencyRules,
  updateAdFrequencyRules,
  listActiveSponsoredPropertyCampaigns,
  listPerfectPartners,
  listPartnerOffers,
  recordAdImpression,
  recordAdClick,
  redeemPartnerOffer,
  listAdPackages,
  getOrCreateAdvertiserForLandlord,
  createSponsoredPropertyCampaign,
  listCampaignsForLandlord,
  getCampaignMetrics,
  listCampaignsForReview,
  reviewCampaign,
  setCampaignStatus,
  updateAdPackage,
  createPerfectPartner,
  updatePerfectPartner,
  createPartnerOffer,
  updatePartnerOffer,
  getAdvertisingRevenue,
  getCurrentRentalForTenant,
  setTenantPaymentSetup,
  getLandlordPayoutAccount,
  connectLandlordPayoutAccount,
  updateLandlordPayoutSchedule,
  getPlatformFeeConfig,
  updatePlatformFeeConfig,
  listLandlordTenantAutopayStatus,
  fileDispute,
  resolveDispute,
  listDisputesForTenant,
  listDisputesForLandlord,
  issueRefund,
  listRefundsForTenant,
  listRefundsForLandlord,
  listRecentWebhookEvents,
  getVerifiedTierConfig,
  updateVerifiedTierConfig,
  getOwnVerifiedPurchase,
  startVerifiedCheckout,
  purchaseVerifiedDirect,
} = impl;

export type {
  AdminMetrics,
  AdvertisingRevenue,
  AuthUser,
  CampaignMetrics,
  CurrentRental,
  LandlordPublicProfile,
  MarketplaceTenant,
  NewLandlordReview,
  NewProperty,
  PassportViewWithViewer,
  PropertyFilter,
  ScoredProperty,
  TenantAutopayStatus,
} from "./types";
